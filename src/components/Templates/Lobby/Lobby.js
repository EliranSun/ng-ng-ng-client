import React from "react";
import { Slider, GameCategories } from "../../Molecules";

const Lobby = () => {
  return (
    <div>
      <Slider />
      <GameCategories />
    </div>
  );
};

export default Lobby;
