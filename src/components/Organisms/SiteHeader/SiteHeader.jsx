import { LoginButton, Logo, RegisterButton } from "../../atoms";

const SiteHeader = () => {
  return (
    <header>
      <Logo />
      <LoginButton />
      <RegisterButton />
    </header>
  );
};

export default SiteHeader;
